import { Controller, Get, Post, Put, Delete, Param, Body } from '@nestjs/common';
import { {{pascalCase module}}Service } from './{{module}}.service';
import { Create{{pascalCase module}}Dto, Update{{pascalCase module}}Dto } from './dto';

@Controller('{{module}}')
export class {{pascalCase module}}Controller {
  constructor(private readonly service: {{pascalCase module}}Service) {}

  @Post()
  create(@Body() dto: Create{{pascalCase module}}Dto) {
    return this.service.create(dto);
  }

  @Get()
  findAll() {
    return this.service.findAll();
  }

  @Get(':id')
  findOne(@Param('id') id: string) {
    return this.service.findOne(+id);
  }

  @Put(':id')
  update(@Param('id') id: string, @Body() dto: Update{{pascalCase module}}Dto) {
    return this.service.update(+id, dto);
  }

  @Delete(':id')
  remove(@Param('id') id: string) {
    return this.service.remove(+id);
  }
}
